apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: action-server-data
  name: action-server-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
