apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: actual-finance-claim0
  name: actual-finance-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
